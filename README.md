# Mohannad 4o - Strict No Duplicate Units FIX

این نسخه مشکل تکرار واحدها را دقیق‌تر فیکس می‌کند؛ مخصوصاً حالت نیم‌فاصله فارسی مثل «میلی‌لیتر».

## نمونه مشکل

```text
کرم چند منظوره کلیون مدل Crema Polivalente حجم 300 میلی‌لیتر 300 ml
```

## خروجی درست

```text
کرم چند منظوره کلیون مدل Crema Polivalente حجم 300 میلی‌لیتر
```

## قانون

- اگر مقدار فارسی و انگلیسی یکی باشند، فقط فارسی نگه داشته می‌شود.
- `300 میلی‌لیتر 300 ml` تبدیل می‌شود به `300 میلی‌لیتر`.
- `250 گرم 250 g` تبدیل می‌شود به `250 گرم`.
- اگر فقط `10.4 oz` وجود داشته باشد، حفظ می‌شود.
- اگر فقط `300 ml` وجود داشته باشد و فارسی‌اش نباشد، حفظ می‌شود.
- اگر `SPF`، `PA` یا `UVA/UVB` تکراری بیاید، یک‌بار نگه داشته می‌شود.

## امکانات حفظ‌شده

- کشور کلاً حذف است.
- لینک داخلی واقعی از دسته‌بندی‌های سایت noon-valqalam.ir
- تشخیص محصول از عکس مهم‌تر از اسم تایپی
- اصلاح اسم محصول
- جزئیات عکس مثل SPF، oz، ml، شماره رنگ و رایحه اگر وجود داشته باشد ثبت می‌شود.
- ساختار Mohannad SEO
- قوانین Yoast SEO
